# @package _global_

datamodule:
  batch_size: 16
  aug:
    apply: False

trainer:
  min_epochs: 80
  max_epochs: 120

model:
  optimizer_options:
    scheduler:
      num_epochs: 120
      max_lr: 0.004
  backbone: resnet50

run_mdl: True
